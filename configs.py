# config.py
class Config:
    # Database URI for SQLite
    SQLALCHEMY_DATABASE_URI = 'sqlite:///database.db'
    #SQLALCHEMY_TRACK_MODIFICATIONS = False  # Turn off Flask's modification tracking
    #SECRET_KEY = 'your-very-secure-random-key'  # Generate a strong secret key for session management
